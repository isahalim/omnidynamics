name = "omnidynamics"
compatibility_date = "2026-09-06"

# Static-assets-only Worker: Astro builds to ./dist, Cloudflare serves it.
[assets]
directory = "./dist"
html_handling = "auto-trailing-slash"
not_found_handling = "404-page"
