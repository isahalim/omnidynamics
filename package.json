{
  "name": "omnidynamics",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-shaders.mjs && astro build",
    "preview": "wrangler dev",
    "check": "astro check",
    "check:shaders": "node scripts/check-shaders.mjs",
    "deploy": "astro build && wrangler deploy",
    "build:icons": "node scripts/build-icons.mjs",
    "build:meshes": "node scripts/build-meshes.mjs"
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.5.0",
    "@gltf-transform/extensions": "^4.5.0",
    "@gltf-transform/functions": "^4.5.0",
    "astro": "^7.3.1",
    "meshoptimizer": "^1.2.0",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "@vgpu/wgsl": "^0.4.0",
    "vgpu": "^0.4.0"
  }
}
